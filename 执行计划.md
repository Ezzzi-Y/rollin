# Rollin V1 执行计划

编制日期：2026-09-19  
需求依据：[需求说明.md](D:/MyWebProject/rollin/需求说明.md)，其中第 88 章优先于前文冲突内容。  
执行范围：基于现有仓库完成滚动录取平台 V1；本轮只编制计划，不修改业务代码。

## 1. 执行结论与边界

现有仓库具备 Go 后端基础、部分录取逻辑和邮件队列，但数据模型与最新澄清存在结构性差异；前端仍为 Vite 初始化页面。执行顺序应为：**统一业务规则和数据模型 → 补齐租户权限与邮件基础 → 完成导入排名及录取事务 → 交付管理端和候选人页面 → 按授权开展验收和部署**。

保留现有 `rollin-backend`、`rollin-frontend` 工程目录，以及 Go、MySQL、Redis、React、TypeScript、Vite 技术方向。需求中的 `rollin-server`、`rollin-web` 是推荐名称，不为目录改名单独增加工作。后端按业务域逐步拆分现有大文件，前端在现有工程内建设。

### Token 方案决策

本项目不采用“所有 Token 统一使用 JWT”的方案。JWT 是一种编码和签名格式，不会自动解决授权、撤销、幂等或租户隔离；本项目的三类业务链接都需要服务端实时判断状态，因此统一使用 JWT 的收益有限，反而会引入过期前难以撤销、Payload 可被读取、URL 过长和密钥轮换等问题。

- 管理后台继续使用需求明确要求的 **Session + Cookie**。Session ID 为随机不可预测值，状态保存在 Redis/服务端，Cookie 使用 HttpOnly、生产环境 Secure 和合理 SameSite。
- Offer、Import、Invite 使用 **高熵不透明随机 Token**，建议至少 32 bytes，数据库只保存 SHA-256 Hash；原文只在创建/发送或首次展示时交付。每类 Token 都绑定作用域、状态和过期时间，由数据库实时检查撤销、活动状态和业务状态。
- Offer Token 允许多个 Token 指向同一个 Offer，终态后全部不可操作；Import Token 可重复使用但可吊销；Invite Token 一次性使用且重复邀请会使旧 Token 失效。这些要求与不透明 Token 的模型直接匹配。
- JWT 仅作为未来明确需要的短时、服务到服务无状态凭证的可选方案；若采用，必须包含 `iss`、`aud`、`sub`、`jti`、`exp`、scope 和活动作用域，并在高风险操作前回查数据库状态。它不能替代 Offer/Import/Invite 的实时状态查询，也不能放宽后台 Session 方案。

因此，后文出现“Token”时，默认指不透明随机 Token；只有明确写出 JWT 的内部服务鉴权场景才使用 JWT。

本计划基于文件阅读和代码审阅，不代表项目已编译、测试或可部署。未连接数据库、Redis、SMTP，未查验实际数据库结构与存量数据。文件清单中未发现项目测试代码、CI 配置或后端使用说明；前端存在 `package-lock.json`，后端存在 `go.sum`。

本期不增加组织层级、年度/届次、报名系统、密码找回、运行中切换录取模式、活动删除、平台 User 禁用或系统内数据库清理功能。负责人/管理员停用通过活动成员权限表达，不扩展成平台账户封禁。

## 2. 仓库现状与需求差距

“已有基础”表示可在重构后复用，不能视为已经验收通过。

| 模块 | 代码证据与现状 | 执行处理 |
| --- | --- | --- |
| 工程基础 | [go.mod](D:/MyWebProject/rollin/rollin-backend/go.mod) 已声明 Go 1.25、chi、GORM、MySQL、Redis、bcrypt 等依赖；[main.go](D:/MyWebProject/rollin/rollin-backend/cmd/server/main.go) 已连接数据库和 Redis、启动 HTTP 与 Worker | 保留技术栈，补配置说明、依赖锁定和可控迁移流程 |
| 活动模型 | [models.go](D:/MyWebProject/rollin/rollin-backend/internal/model/models.go:51) 使用 `Admission`；[CreateAdmission](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:132) 强制先指定负责人并创建 `DRAFT` 活动 | 对齐 Activity 语义；创建时可无负责人；拆开生命周期和“是否正式启动” |
| 账户与成员 | [AdminUser](D:/MyWebProject/rollin/rollin-backend/internal/model/models.go:87) 邮箱全局唯一、角色放在账户上；[Login](D:/MyWebProject/rollin/rollin-backend/internal/auth/auth.go:70) 不接收目标活动 | 改为活动内独立账户、活动内邮箱唯一、Session 绑定目标活动；平台登录单独处理 |
| 权限基础 | [router.go](D:/MyWebProject/rollin/rollin-backend/internal/httpapi/router.go) 已隔离平台路由和活动业务入口，明确拒绝超级管理员进入业务；成员关联已有雏形 | 保留隔离方向，补成员停用、活动状态、归档只读及资源归属检查 |
| 会话状态 | [Session](D:/MyWebProject/rollin/rollin-backend/internal/auth/auth.go:99) 从 Redis 读取并续期；未重新验证成员有效性、活动状态 | 停用成员后旧 Session 必须失去对应权限，不能只在登录时检查 |
| 邀请 | [Invite / ResendInvitation](D:/MyWebProject/rollin/rollin-backend/internal/auth/auth.go:176) 已有一次性邀请、重新生成 Token 与队列；有效期默认 168 小时，邀请尚未绑定完整活动上下文 | 默认改为 72 小时；同活动成员去重；区分负责人和管理员邀请的 SMTP 来源 |
| Candidate | [Candidate](D:/MyWebProject/rollin/rollin-backend/internal/model/models.go:66) 按邮箱全局唯一，姓名/邮箱/电话放在共享记录上 | 增加全局唯一、不可修改的 `student_id` 和 `accepted_offer_id`；姓名/邮箱移入 Application |
| Application / 排名 | [Application](D:/MyWebProject/rollin/rollin-backend/internal/model/models.go:73) 有活动候选人唯一约束和 rank；缺少 score、导入顺序、排名待重算与冻结信息 | 新增正整数 score、稳定导入顺序、排名重算/同分调整/永久冻结机制；独立 Application 状态 |
| 导入 | [ImportCandidates](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:212) 通过后台 Session 导入数组，外部提交 rank，按邮箱识别人 | 替换为 Import Token 授权的单人 API；按学号幂等写入；禁止外部指定 rank |
| AUTO / MANUAL | [fill](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:105) 已按 rank 补位；[IssueOffer](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:233) 有容量检查但没有强制 MANUAL、排名冻结和全局资格检查 | 保留容量锁定思路，统一所有发放入口的业务前置条件 |
| 跨活动互斥 | [transition](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:78) 只处理当前活动/Offer，无 Candidate 分布式锁和全局最终录取状态 | 重构 Accept 事务、跨活动自动放弃和提交后可靠递补 |
| GET 副作用 | [GetOffer](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:67) 在读取过期 Offer 时调用 `expireOne`，会修改状态并可能递补 | GET 全程只读；过期提示可以计算，状态落库交给写操作/后台任务 |
| Offer 唯一约束 | [Offer](D:/MyWebProject/rollin/rollin-backend/internal/model/models.go:76) 的 `uk_offer_current` 只标记在 `IsCurrent`，没有和 `ApplicationID` 组成联合索引 | 按当前模型建表会产生全表 current 值唯一的风险；优先修正为每个 Application 的有效 Offer 约束，并核实实际索引 |
| Token 与重发 | Offer 在核心事务中生成 Token，同时保存 Hash 与密文；[ResendOffer](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:235) 将普通重发和过期后新建 Offer 混在一起 | 新增一对多 OfferToken；发送时生成、只存 Hash；普通邮件重发与负责人特殊重新发放分开 |
| 邮件 | [worker.go](D:/MyWebProject/rollin/rollin-backend/internal/mail/worker.go) 已在事务外发送、支持抢占及退避重试；所有邮件使用同一环境 SMTP，未充分检查活动/Offer 状态 | 补平台/活动 SMTP、模板、发送前检查、取消状态、人工重排队；纠正跳过邮件也写入发送时间的问题 |
| 生命周期 | [Activate / expireOne](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go:120) 使用旧 `DRAFT/ACTIVE/CLOSED` 语义；过期任务会先改变 Offer，再判断是否递补 | 实现 ACTIVE / DISABLED / ARCHIVED；禁用与归档必须约束所有写入口及后台任务 |
| 配置与业务视图 | [settings.go](D:/MyWebProject/rollin/rollin-backend/internal/settings/settings.go) 有平台参数；Dashboard 一次预加载全部候选记录 | 补活动设置、SMTP/模板、分页查询、独立统计、邮件管理、XLSX 导出和审计查询 |
| 前端 | [App.tsx](D:/MyWebProject/rollin/rollin-frontend/src/App.tsx) 仍为示例页面；[package.json](D:/MyWebProject/rollin/rollin-frontend/package.json) 尚无业务路由、查询、表单及 UI 依赖 | 建立前端结构、三类管理权限页面、邀请激活页及正式候选人 Offer 页面 |
| 部署 | [docker-compose.yml](D:/MyWebProject/rollin/docker-compose.yml) 仅含后端；[Nginx 示例](D:/MyWebProject/rollin/deploy/nginx.conf.example) 有两个域名，但候选人域名代理整个 public 前缀、共用 SPA，且没有 Import API 入口 | 明确外部 MySQL/Redis 依赖、前端产物、Import 路由和域名访问白名单，补部署/恢复说明 |

## 3. 必须先固定的实现规则

### 3.1 身份、权限与生命周期

1. Activity 使用 `ACTIVE / DISABLED / ARCHIVED`。名单准备与录取启动由 `ranking_dirty`、`ranking_frozen`、`started_at` 等字段表达，不再借活动状态表示。
2. 一个活动可以暂时没有负责人；负责人建立后只能有一个 OWNER，可以有多个 ADMIN。正式启动前必须有有效负责人。
3. 活动 User 按“活动 + 规范化邮箱”唯一，不跨活动复用账户或密码。平台超级管理员使用独立认证作用域；ActivityMember 保存活动角色和成员状态。
4. 登录请求、Session 和后端权限检查必须共同确定目标活动；不能仅依赖前端传入一个可随意切换的 Activity ID。
5. 超级管理员只能访问平台配置、活动生命周期、负责人管理及活动数量统计，不返回候选人、分数、排名、Offer、录取结果或详细业务统计。
6. DISABLED 拒绝成员进入、拒绝 Public Offer 访问并显示“Offer 已失效”，停止过期/递补，取消未发送邮件；活动重新激活不复活已取消的邮件任务。
7. ARCHIVED 不可重新激活，允许有效成员登录、查询和导出；Offer 页面只展示结果，所有业务修改及邮件/递补/过期任务停止。
8. 重新激活与恢复发放分开：增加“递补暂停/待人工恢复”标记或等价机制，防止过期 Worker、增加 quota 等入口绕过“人工触发补发”的要求。AUTO 恢复动作仍按 rank 递补，不能变成人工挑人。

### 3.2 数据与录取不变量

| 对象 | 目标约束 |
| --- | --- |
| Candidate | `student_id` 全局唯一且不可变；按字符串保存以保留前导零；只承载共享身份与最终接受指针，不共享活动内姓名、邮箱、分数 |
| Application | `UNIQUE(activity_id, candidate_id)`；保存 name、email、score、rank、状态、稳定导入顺序；score 为 `1..2147483647` 的整数 |
| 排名 | 导入/修改成绩后标记待重算；人工重算按 `score DESC, import_order ASC`；仅同分组可调整；启动时排名完整且无待重算标记；启动后永久冻结 |
| Application 状态 | `WAITING / OFFERED / ACCEPTED / DECLINED / EXPIRED / INELIGIBLE`，不再复用 Offer 的 PENDING 作为 Application 状态 |
| Offer 状态 | 普通路径为 `PENDING → ACCEPTED / DECLINED / EXPIRED`；终态不恢复；历史 Offer 保留 |
| 容量 | 所有发放、特殊重新发放、quota 修改与递补，在事务内满足 `ACCEPTED + PENDING <= quota`；quota 必须大于 0 |
| 全局接受 | 一个 Candidate 最多接受一个 Offer；Redis Candidate 锁之外，MySQL 事务重新检查并条件更新 `accepted_offer_id` |
| 发放资格 | AUTO/MANUAL/特殊操作都不能绕过全局已接受检查；已接受其他活动的候选记录标记为 INELIGIBLE，不能继续普通发放 |
| AUTO | 只按冻结后的 rank 选择合格 WAITING 候选人；不再用 score 决定正式发放顺序 |
| MANUAL | 正式启动并冻结后，OWNER/ADMIN 可以选择合格候选人；放弃、超时和其他活动接受均不自动补位 |

Application 与 Offer 的更新必须在同一事务内保持一致。一个 Application 可以对应多个历史 Offer，但只能有一个占用名额的有效 Offer；统计必须明确使用 Offer 口径，避免把历史重新发放重复计入当前占用。

### 3.3 Token、邮件及幂等

1. Import Token 仅创建时返回原文，数据库只存 Hash；只能放入 Authorization Bearer，绑定唯一活动，只能调用单人导入接口，可重复使用、过期和吊销。
2. Offer 核心事务只创建 Offer、MailTask 和必要业务事件，不生成 Token、不访问 SMTP。
3. Mail Worker 在实际发送尝试前生成随机 Offer Token，短事务保存 `OfferToken(offer_id, token_hash, created_at)`，事务提交后再发送。原文仅用于本次邮件，不保存可逆 Offer Token 密文或包含原文的邮件正文。
4. 重试可以生成新 Token；已有 Token 仍指向同一个 Offer。所有 Token 的操作权限由 Offer 状态、截止时间和活动状态共同决定，不能因生成新 Token 恢复终态 Offer。
5. Offer 终态 Token 可以展示已处理结果；禁止再执行状态变更。重复 Accept/Decline 返回实际最终结果，不重复写审计、发邮件或触发递补。
6. GET 页面/API 不修改 Offer、Application、MailTask 或录取事件。数据库尚未结算过期时，响应可以携带计算出的“已过期、不可操作”结果。
7. 邮件普通重发只对原 Offer 排队，不改变状态和截止时间；重新给予机会使用单独 OWNER 接口，必须有原因，新建 Offer 并保留旧终态。
8. SMTP 密码使用服务端密钥加密，返回配置时只返回脱敏信息。有效性验证绑定配置版本；配置修改后不能沿用旧的验证成功结果。
9. 邮件按至少一次投递设计：SMTP 成功后进程崩溃可能导致重复邮件，但不能导致重复 Offer 或重复录取。发送状态、业务状态分别展示。

## 4. 目标结构与数据改造

以下为目标职责，不要求一次性建立大量空目录。

| 当前入口 | 计划拆分/新增职责 |
| --- | --- |
| [internal/service/service.go](D:/MyWebProject/rollin/rollin-backend/internal/service/service.go) | 按 activity、application、ranking、offer、importtoken、audit、export 拆分；事务边界留在业务服务，HTTP 层只做请求转换和权限入口 |
| [internal/auth/auth.go](D:/MyWebProject/rollin/rollin-backend/internal/auth/auth.go) | 平台/活动登录、成员授权、Session、邀请；引入统一活动访问策略 |
| [internal/model/models.go](D:/MyWebProject/rollin/rollin-backend/internal/model/models.go) | 分开账户、成员、候选人、报名记录、Offer、Token、SMTP、模板、邮件和审计模型 |
| [internal/db/migrate.go](D:/MyWebProject/rollin/rollin-backend/internal/db/migrate.go) | 增加版本化迁移，显式处理旧索引、列移动和数据转换；不再把生产升级完全交给 AutoMigrate |
| [internal/mail/worker.go](D:/MyWebProject/rollin/rollin-backend/internal/mail/worker.go) | SMTP 配置选择、模板渲染、邮件发送、任务认领/租约/重试/取消；与过期、递补任务分别组织 |
| [internal/httpapi/router.go](D:/MyWebProject/rollin/rollin-backend/internal/httpapi/router.go) | 按平台管理、活动管理、认证、Public Offer、Import 五类接口拆分 |
| [前端 src](D:/MyWebProject/rollin/rollin-frontend/src) | 建立 router、api、layouts、components、hooks，以及 platform、activity、offer、auth 页面目录 |

需要新增或重点调整的持久化对象：

- Activity：生命周期、quota、offer_mode、offer_expire_duration、ranking_dirty、ranking_frozen、started_at、成功提示及恢复递补控制信息。
- User / ActivityMember：活动账户作用域、作用域内邮箱唯一、成员角色/状态；平台管理员单例约束。
- Candidate / Application：student_id、accepted_offer_id、活动内姓名/邮箱/score、稳定导入顺序、可暂空的 rank、独立状态。
- Offer / OfferToken：一对多历史 Offer、一对多 Token；将有效 Offer 唯一约束限定到 Application，清理原全局 `is_current` 唯一索引。
- ImportToken：活动、Hash、名称、状态、有效期、吊销时间、最近使用时间。
- InviteToken/Invitation：活动及成员上下文、一次性状态、72 小时默认有效期；新邀请失效旧链接和旧待发任务。
- SMTPConfig / MailTemplate：平台和活动作用域、加密凭证、配置验证信息、模板类型及版本。
- MailTask：活动/平台作用域、业务对象、收件地址快照、状态、重试、租约、最后错误、取消原因；状态统一为 PENDING / SENDING / SENT / FAILED / CANCELLED 或等价集合。
- AuditLog：平台/活动作用域、操作者、目标、变更摘要、请求关联；业务审计与 HTTP 访问日志区分。
- 递补任务/事务事件：推荐用数据库记录需重试的递补意图，保证 Accept 提交后进程崩溃也不会永久漏补；先不增加独立消息中间件。

数据库索引围绕活动内候选分页、候补 rank 顺序、Offer 截止扫描、MailTask 认领、Token Hash 查询和审计时间查询建立。rank 交换需采用不会中途违反唯一约束的更新方式；排名重算与导入、启动共用活动级事务保护。

## 5. 分阶段任务与完成标准

各阶段的“验收”均为后续计划，不表示已执行。实现阶段可以编写必要测试；测试、构建及其他命令须按第 9 节取得具体授权后运行。

### P0：冻结业务口径和接口契约

**前置依赖：** 无。**优先级：** 最高。

- [ ] P0-1 按第 88 章建立状态迁移表、角色权限表和需求追踪表，记录第 8 节待确认问题。
- [ ] P0-2 确定 `Admission → Activity` 的映射方式。建议新业务接口统一使用 activities；旧物理表是否改名由迁移方案决定，不为统一命名冒险丢数据。
- [ ] P0-3 固定平台与活动登录入口、分页/筛选、错误码、时间格式、Token 公共响应和 OpenAPI 契约。
- [ ] P0-4 盘点是否已有外部 API 调用方及存量数据库；未确认前不假设数据库为空，也不默认保留不合规旧接口。
- [ ] P0-5 列出前端新增依赖、XLSX 生成依赖及测试依赖；实现时核对所选版本的官方文档，不顺带升级现有技术栈。

**交付物：** 状态/权限/API 设计文档、迁移分支方案、待决事项清单。  
**完成标准：** 后续每个写入口都能找到明确的角色、生命周期、状态前置条件及错误响应；核心冲突有结论或明确阻塞范围。

### P1：重建领域模型和迁移基础

**前置依赖：** P0 中身份、状态和数据迁移策略已确定。**优先级：** 最高。

- [ ] P1-1 调整 Activity、User、ActivityMember、Candidate、Application、Offer 模型，增加第 4 节所需新实体。
- [ ] P1-2 修正邮箱唯一作用域、student_id 唯一约束、Application 唯一约束及每个 Application 有效 Offer 唯一约束；修复 `uk_offer_current`。
- [ ] P1-3 分离 Application/Offer/MailTask 状态类型；加入 score、quota、rank 和时间字段的校验规则。
- [ ] P1-4 增加版本化迁移及升级前检查，避免“更新模型标签却保留旧索引”的情况。
- [ ] P1-5 将基础模型变化贯穿编译所需的调用点，逐步拆分业务文件，不留下新旧规则并存的公开写入口。

**存量数据处理要求：**

- 空数据库：建立符合新需求的初始结构，仍需通过正式初始化流程。
- 有数据：先备份、生成映射报告，再迁移。旧 Candidate 没有学号、旧 Application 没有 score，不能从邮箱/rank 猜测补值；必须由可靠来源补齐，无法补齐的记录阻止切换。
- 旧跨活动账户拆分时明确密码和邀请迁移策略，并使旧 Session 失效；不能因邮箱相同重新合并。
- `DRAFT/ACTIVE/CLOSED` 与新生命周期、启动标记不能仅凭名称机械转换，需结合已有 Offer/排名和业务确认。
- 已发出的旧 Token 如需保持可用，将 Hash 迁入 OfferToken 并保留原 Offer 关联；切换后停止依赖 Offer Token 密文。是否保留旧邀请链接单独决定。

**交付物：** 新模型、迁移文件、升级/回退说明、迁移前置检查清单。  
**完成标准：** 可以描述并验证多活动同邮箱账户、同学号跨活动报名、多个历史 Offer 和多个有效 Token；迁移不猜测关键业务字段。

### P2：账户、成员权限及活动生命周期

**前置依赖：** P1。邀请实际发送依赖 P3，接口可先与发信队列解耦。**优先级：** 最高。

- [ ] P2-1 平台超级管理员幂等初始化，处理多实例同时启动；活动登录必须确定目标 Activity。
- [ ] P2-2 Session 保存账户和认证作用域；授权时检查成员及活动实时状态，完善退出和停用后的权限失效。
- [ ] P2-3 活动可以先创建、后邀请唯一负责人；负责人在当前活动邀请多个管理员。禁止跨活动账户复用和重复成员关系。
- [ ] P2-4 邀请默认 72 小时，重复邀请产生新链接并撤销旧链接；邀请接受事务内复查状态和截止时间，避免并发重邀后旧链接仍可用。
- [ ] P2-5 完成平台活动列表/数量统计、禁用、重新激活；按 P0 决策实现归档。成员停用仅作用于当前活动。
- [ ] P2-6 将活动状态策略应用于管理员 API、Import、Offer 公共接口、邀请激活、邮件、过期与递补任务。
- [ ] P2-7 Cookie 保持 HttpOnly、生产 Secure 和合理 SameSite；为后台写请求加入明确的 CSRF/Origin 防护，补登录及 Token 入口限流。

**交付物：** 可用的认证/邀请接口、权限中间件、生命周期接口和相关审计。  
**完成标准：** 相同邮箱可在不同活动使用独立密码；伪造活动 ID 或资源 ID 不能越权；超级管理员不能读取业务；禁用/归档后的所有入口遵循统一规则。

### P3：分作用域 SMTP、模板和可靠邮件

**前置依赖：** P1；状态检查依赖 P2。**优先级：** 高；P4 可在数据基础完成后独立推进。

- [ ] P3-1 超级管理员配置平台 SMTP，OWNER 配置活动 SMTP；提供配置检查/测试、凭证加密、脱敏返回和配置变更审计。
- [ ] P3-2 负责人邀请走平台 SMTP；管理员邀请、Offer 和活动通知走当前活动 SMTP；不得隐式回退到其他活动或平台发信账号。
- [ ] P3-3 SMTP 未验证有效时，阻止依赖它的业务入队，并提供可理解的错误原因；配置失效后 Worker 保留失败与重试信息。
- [ ] P3-4 支持活动邮件模板和确认成功提示，OWNER/ADMIN 权限符合需求；限制模板变量、处理内容转义，避免在模板中暴露其他活动信息。
- [ ] P3-5 Worker 认领后、生成 Token 前及发送前重新检查活动、业务对象和任务是否仍可发送；过期/终态 Offer、失效邀请、禁用/归档活动均不得新发邮件。
- [ ] P3-6 将 Offer Token 生成移至发送尝试；实现多个 Hash 对应同一 Offer，重试不延长截止时间。
- [ ] P3-7 实现租约认领、过期租约恢复、失败退避、FAILED 人工重排队、取消与条件更新，防止旧 Worker 覆盖 CANCELLED 或新租约结果。
- [ ] P3-8 补 SMTP 超时、发送结果分类及可观测信息；只有实际成功发送才设置 sent_at，跳过/取消不能记为已发送。

**交付物：** SMTP/模板接口、可靠 Worker、邮件状态和重试接口。  
**完成标准：** 各活动使用自己的发信配置；业务事务不调用 SMTP；禁用后未发任务取消且不会随激活复活；重复投递不产生重复 Offer。已进入 SMTP 传输的邮件无法追回，应在实现说明中界定并测试禁用与发送的竞态边界。

### P4：Import Token、单人导入及排名冻结

**前置依赖：** P1、P2。**优先级：** 高。

- [ ] P4-1 OWNER 创建/查询/吊销 Import Token；创建后仅展示一次原文，记录有效期和使用情况。
- [ ] P4-2 新建 `POST /api/import/candidates`，只接受 `{student_id, name, email, score}` 单对象；拒绝数组、URL Token、额外指定活动或 rank。
- [ ] P4-3 在事务中按 student_id 查找/创建 Candidate，按活动+Candidate 幂等写入 Application；跨活动仅共享身份，不覆盖其他活动资料。
- [ ] P4-4 明确幂等更新策略：建议未冻结时允许更新当前 Application 的 name/email/score；同内容重试返回已有结果，不重复生成导入顺序。按需求每次有效导入标记排名待重算，调用方需在导入完成后统一重算。
- [ ] P4-5 提供分页名单和成绩修改接口；修改 score 标记待重算。仅允许正整数，student_id 不可修改。
- [ ] P4-6 人工重算按 score 降序、导入顺序升序生成连续 rank；同分调整校验组内完整性、越界和重复 ID。
- [ ] P4-7 重算与同分调整使用活动级锁/版本控制；明确重算会恢复同分导入顺序，前端在操作前说明对既有同分调整的影响。
- [ ] P4-8 冻结后拒绝所有导入（包括重试请求）、成绩/排名修改和报名删除；旧 Session 批量导入接口退出使用，不能成为绕过途径。

**交付物：** 导入 Token 管理、单人 API、调用说明、排名服务。  
**完成标准：** 并发重试不重复建人/建报名；相同学号跨活动资料隔离；排名待重算时不能启动；冻结后无法通过任意接口改动排名基础。

### P5：录取引擎、跨活动接受及可靠递补

**前置依赖：** P1—P4，且第 8 节涉及跨状态互斥的决策已确定。**优先级：** 最高，属于核心风险阶段。

- [ ] P5-1 将旧 Activate 拆为“平台重新激活”和“OWNER 正式启动录取”；启动事务检查活动状态、负责人、SMTP、quota、完整排名和待重算标记，原子冻结排名并记录审计。
- [ ] P5-2 AUTO 首发/补位在活动级锁下按 rank 选择 WAITING，检查 Candidate 全局资格，创建 Offer + MailTask；MANUAL 启动只冻结排名，人工发放另行检查全部前置条件。
- [ ] P5-3 Accept 使用 Candidate 级 Redis 锁，处理租约、持有者校验释放、超时与失败；Redis 不可用时返回可重试失败，不绕过需求中的锁步骤。
- [ ] P5-4 Accept 数据库事务重新检查 Candidate、活动、Offer、截止时间及 `accepted_offer_id`，原子写入本 Offer ACCEPTED、Candidate 最终指针、其他 PENDING 的 DECLINED 与相关 Application/审计。
- [ ] P5-5 制定 Accept、发放、Decline、过期、quota 修改共同遵守的数据库锁顺序；多活动操作按稳定顺序加锁，对目标集合变化重新检查，对死锁做有界重试。发放与 Accept 也必须串行校验 Candidate，不能只串行化两个 Accept。
- [ ] P5-6 将受影响活动的递补意图与状态更新同事务持久化，提交后执行/重试；同活动串行补位，多次执行不超 quota、不重复 Offer，不能只依赖一次内存回调。
- [ ] P5-7 完成 Decline/Expire 幂等迁移，正常 AUTO 递补、MANUAL 仅释放容量；DISABLED/ARCHIVED 停止业务修改；重新激活后先结算到期状态，发放仍受人工恢复标记约束。
- [ ] P5-8 Public GET 改成纯查询，返回服务器时间、截止时间、显示状态、可执行动作及成功提示；POST 重复提交返回既有最终结果。
- [ ] P5-9 分离普通邮件重发和 OWNER 特殊新 Offer：后者必须填写原因、保留旧 Offer，并执行 quota、SMTP、活动状态和 Candidate 全局资格检查。
- [ ] P5-10 quota 调整与发放使用相同活动事务边界；容量增加是否即时补位遵循已固定策略，不能绕过暂停和冻结条件；正式启动后禁止切换 AUTO/MANUAL。

**交付物：** 完整录取状态机、并发控制、公共 Offer API、补位恢复机制、负责人特殊操作。  
**完成标准：** 同一 Candidate 并发接受不同活动仅一个成功；并发补位永不超容量；GET 无状态修改；进程在 Accept 提交后中断仍可继续补位；已处理 Offer 不能被重发或旧 Token 重新激活。

### P6：查询、统计、审计及 XLSX 导出

**前置依赖：** P2、P4、P5；邮件字段依赖 P3。**优先级：** 高。

- [ ] P6-1 将 Dashboard 聚合和候选人列表拆开，避免一次读取全部 Application；支持查询、状态筛选、rank/score 排序及稳定分页。
- [ ] P6-2 统计 quota、ACCEPTED、PENDING、DECLINED、EXPIRED、WAITING、历史已发 Offer 数、当前占用、失败邮件数、活动状态、模式和排名冻结情况。
- [ ] P6-3 Offer 列表区分当前与历史，邮件列表区分任务失败与 Offer 状态；已失去资格的候选记录有明确说明，不暴露其接受了哪个其他活动。
- [ ] P6-4 XLSX 同步生成并下载，只导出当前活动的数据；包含学号/候选标识、姓名、邮箱、score、rank、状态和发送/接受/放弃/过期时间。学号按文本输出，避免前导零丢失及公式解释。
- [ ] P6-5 OWNER 可分页查询本活动审计；平台审计与活动业务审计隔离。关键成功操作和系统状态变更应有事务内审计，不能用包含所有 GET 的通用请求日志替代。
- [ ] P6-6 明确“已发 Offer 总数”与“收到过 Offer 的候选人数”的差别；特殊重新发放不得扭曲当前占用。前端定时刷新及写操作后刷新提供近实时统计，刷新目标在 P0 固定。

**交付物：** 查询/统计/导出/审计接口及数据口径说明。  
**完成标准：** 列表、统计和导出在相同口径下相互一致；归档活动仍可导出；没有任何跨活动数据泄露。

### P7：管理端与候选人端交付

**前置依赖：** P0 契约确定即可开始页面结构；真实联调依赖 P2—P6。**优先级：** 高，候选人页面优先开展视觉设计。

- [ ] P7-1 接入 React Router、TanStack Query、Tailwind CSS、shadcn/ui、React Hook Form、Zod、Lucide 等需求建议能力；按已批准的依赖清单实施，不在本轮安装。
- [ ] P7-2 建立 API 客户端、认证状态、统一错误/加载/空态、权限路由、表格/表单组件；前端权限表现与后端能力保持一致。
- [ ] P7-3 完成平台登录与活动登录（活动标识明确）、邀请激活、退出、失效邀请等页面，不提供密码找回。
- [ ] P7-4 平台后台：活动创建/列表/状态管理、负责人邀请/停用、平台 SMTP、平台参数、活动数量统计；不渲染内部招新数据。
- [ ] P7-5 OWNER 工作区：Dashboard、候选人、排名、Offer、邮件、活动设置、SMTP、成员、Import Token、导出和审计。
- [ ] P7-6 ADMIN 工作区：业务查看、允许的排名准备操作、MANUAL 发放、邮件失败重试、模板/成功提示、导出；无 quota、SMTP、成员管理、Import Token 和正式启动权限。
- [ ] P7-7 候选人 Offer 页采用独立布局，展示正式录取内容、姓名、活动、截止时间和操作；接受再次确认，放弃明确二次确认；覆盖已接受、已放弃、已过期、无效、禁用和归档状态。
- [ ] P7-8 完成移动端适配、键盘可用性、提交中防重复、服务器时间校准、结果刷新；仅加载页面不得调用写 API。
- [ ] P7-9 候选人域名只提供 Offer 路由和所需公共 API，不提供登录、后台、邀请激活或活动管理入口；支持直接访问深层路由。

**交付物：** 三类管理页面、邀请激活页、完整候选人 Offer 页面和统一视觉规范。  
**完成标准：** 用户可以从邀请进入后台完成招新全流程；候选人可在手机端完成一次确认并看到稳定结果；错误、权限和活动状态有清晰反馈。

### P8：回归验收、部署与交接

**前置依赖：** P1—P7 完成，用户批准具体执行命令，具备隔离验收环境。**优先级：** 上线门禁。

- [ ] P8-1 执行第 7 节核心验收矩阵，使用实际 MySQL/Redis 验证行锁、唯一约束和多实例竞态；不能只用 Mock/内存数据库代替并发验证。
- [ ] P8-2 覆盖 SMTP 失败、任务重试、Worker 重启、Redis 故障、数据库回滚、Accept 提交后崩溃及活动禁用与发送并发。
- [ ] P8-3 补安全检查：租户越权、后台写请求伪造、Token 暴露、URL/日志脱敏、模板内容处理、导出范围和密钥配置。
- [ ] P8-4 补前端生产构建交付路径、容器配置、健康/就绪检查、TLS、后台域名、候选人域名和 Import API 路由；后端端口不作为绕过域名限制的公共入口。
- [ ] P8-5 根据现有部署方式明确 MySQL/Redis 使用外部服务；如需本地开发容器，单独提供开发配置，不默认修改生产资源。
- [ ] P8-6 编写根 README、后端配置示例、导入 API 示例、部署步骤、迁移/备份/恢复方案、值班排障说明；示例中不保存真实凭证。
- [ ] P8-7 形成验收记录：已执行命令、环境、关键场景结果、缺陷和限制。未执行的项目明确标记，不以静态审阅代替运行结论。

**交付物：** 可部署产物、验收记录、操作手册和回退方案。  
**完成标准：** 核心不变量和跨活动隔离全部通过；业务人员能够独立完成配置、导入、排名、发放、处理失败邮件及导出。

## 6. 执行顺序与接口覆盖

### 6.1 阶段依赖与里程碑

| 里程碑 | 包含阶段 | 可交付能力 |
| --- | --- | --- |
| M0：规则与数据基础 | P0 → P1 | 明确状态/权限/迁移，具备新模型与接口契约 |
| M1：活动可管理 | P2 + P3 | 平台创建活动、邀请负责人、活动成员登录、独立 SMTP |
| M2：名单可冻结 | P4 | Token 单人导入、排名重算、同分调整及启动条件检查 |
| M3：录取闭环 | P5 | AUTO/MANUAL 发放、邮件、公开确认、跨活动互斥和可靠递补 |
| M4：产品可交付 | P6 + P7 | 管理后台、候选人体验、统计、审计和 XLSX 导出 |
| M5：具备上线依据 | P8 | 并发/故障/权限验收完成，部署及回退材料齐备 |

后端主线是 P0 → P1 → P2/P3/P4 → P5 → P6 → P8。前端可在 P0 契约完成后建设布局与页面状态，随后按已完成接口逐步联调。这里描述的是工程依赖，不代表本轮会启动并行代理或执行实现。

在团队人数、存量数据及部署条件确定前，不承诺具体上线日期。排期时优先预留 P1 数据迁移和 P5 并发一致性的风险缓冲，不用压缩这两部分换取页面进度。

### 6.2 接口职责清单

以下是计划中的接口族，完整方法、路径和字段在 P0 固定。

| 接口族 | 授权方式 | 必须覆盖 |
| --- | --- | --- |
| 平台认证/配置 | 平台 Session | 登录、退出、当前账户、平台参数、平台 SMTP |
| 平台活动管理 | SUPER_ADMIN | 创建/查询活动、数量统计、生命周期管理、负责人邀请和成员停用 |
| 活动认证/邀请 | 活动标识 + 邮箱密码；一次性 Invite Token | 活动登录、设置密码、邀请状态、旧邀请失效 |
| 活动业务管理 | 活动 Session + ActivityMember | 查询、成员、设置、SMTP、模板、成功提示、导入 Token、排名、正式启动、quota |
| 活动 Offer 管理 | OWNER/ADMIN + 操作级权限 | MANUAL 发放、普通重发、失败重排队、人工恢复递补；OWNER 特殊新 Offer 独立接口 |
| Public Offer | Offer Token，不依赖后台 Cookie | `GET /api/public/offers/{token}`、`POST .../accept`、`POST .../decline` |
| 外部导入 | Import Bearer Token | `POST /api/import/candidates`，单对象、幂等、活动从 Token 确定 |
| 数据读取/导出 | 当前活动成员 | Dashboard、候选/Offer/邮件分页、XLSX 下载、OWNER 审计查询 |

旧 `/api/admin/admissions/{id}/candidates/import` 的批量 Session 导入，以及混合“激活/启动”“重发/重新给机会”的旧语义必须明确退役。若存在外部消费者，制定短期迁移窗口；不能通过兼容层继续保留与需求冲突的行为。

## 7. 验收矩阵

这些场景用于后续设计测试及业务验收；本轮均未运行。

| 编号 | 场景 | 预期结果 | 对应阶段 |
| --- | --- | --- | --- |
| A01 | 同邮箱在 A、B 两活动注册并设置不同密码 | 两账户独立，A 密码/Session 不能进入 B | P1、P2 |
| A02 | 超级管理员或其他活动成员直接请求候选/Offer/导出接口 | 后端拒绝，错误响应不泄露业务数据 | P2、P6 |
| A03 | 成员停用后使用原 Session；活动禁用后使用旧 Token | 即时拒绝相应访问；不改 Offer/Application 状态 | P2、P5 |
| A04 | 重复邀请、同时接受旧邀请、超过 72 小时后激活 | 旧/过期 Token 不可激活；成员关系不重复 | P2、P3 |
| A05 | 同学号跨活动导入不同姓名/邮箱/分数 | 共享 Candidate，Application 资料互不覆盖 | P1、P4 |
| A06 | 并发重复导入同一人，提交 0/负数/溢出成绩或数组 | 合法数据不重复；不合法请求拒绝；不接受外部 rank | P4 |
| A07 | 导入后直接启动、同分调整、跨分调序、冻结后写入 | 待重算阻止启动；仅同分调整成功；冻结后全部拒绝 | P4、P5 |
| A08 | quota=2，两个有效 PENDING；并发人工发放/增加占用/减少 quota | 始终保持 ACCEPTED+PENDING≤quota，不能将 quota 减至占用以下 | P5 |
| A09 | 两活动同时为同一 Candidate 创建 PENDING | 允许同时等待；不是见到 PENDING 就互相排斥 | P5 |
| A10 | Candidate 并发接受两个活动 Offer；同 Offer 通过不同 Token 重复接受 | 全局仅一个 ACCEPTED；重复请求无重复业务副作用 | P5 |
| A11 | AUTO 多人同时放弃/超时/被其他活动录取 | 按 rank 补满或候补耗尽，不重复发、不超额 | P5 |
| A12 | MANUAL 放弃/超时；在 AUTO 调用人工挑人发放 | MANUAL 不自动递补；AUTO 拒绝普通人工挑选 | P5 |
| A13 | 邮件扫描器反复 GET 有效/过期 Offer | 无业务状态、审计事件或递补队列变化 | P5 |
| A14 | SMTP 失败重试，后使用不同邮件中的多个 Token | 仍为同一 Offer、同一截止时间；任一成功处理后其余不可再次处理 | P3、P5 |
| A15 | EXPIRED 普通重发；ADMIN 特殊发放；OWNER 带原因重新给予机会 | 前两者拒绝；OWNER 符合全部约束时创建新 Offer，保留旧终态及审计 | P5 |
| A16 | Accept 已提交、补位前进程崩溃，随后恢复 | 递补意图可恢复，重复执行保持幂等 | P5、P8 |
| A17 | 活动禁用、重新激活且存在到期/未到期 Offer | 禁用期间暂停且取消待发邮件；恢复后有效链接可用，到期不可接受，不自动补发 | P2、P3、P5 |
| A18 | 归档后登录、导出、修改模板、接受 Offer、运行 Worker | 可读/可导出；写操作与业务 Worker 拒绝；不能重新激活 | P2、P6、P7 |
| A19 | XLSX 中学号带前导零、姓名包含特殊字符，多次历史 Offer | 学号按文本保留，文件可打开，历史记录和当前占用口径正确 | P6 |
| A20 | 手机打开 Offer，确认/放弃/重复提交/刷新结果 | 内容清晰、操作有确认和反馈、终态稳定，无管理入口 | P7 |
| A21 | 从候选人域名访问后台/登录/邀请接口，或请求 Import 未配置路由 | 公共域名严格隔离；正式配置的导入入口可达并单独验证 Bearer | P7、P8 |
| A22 | 检查数据库、API 返回和日志中的 SMTP 凭证、Import/Offer Token | 无明文凭证或原始 Token 泄漏；公开页面 URL 的 Token 不进入普通访问日志 | P3、P4、P8 |

测试覆盖应围绕状态不变量、并发、权限和故障恢复，避免为简单展示组件或低风险文案编写只重复实现逻辑的测试。使用专用测试数据和测试发信服务，不向真实候选人发送验收邮件。

## 8. 实施前待确认事项与风险处理

下列内容是尚需固定的业务/环境边界，不将建议写成已经确认的需求。可先完成不受影响的模型和接口设计，相关行为在决策完成前不能宣布交付。

| 编号 | 待确认问题 | 建议与影响 | 最晚决策点 |
| --- | --- | --- | --- |
| D1 | Candidate 接受 A 时，B 已 DISABLED/ARCHIVED 且存在 PENDING：88.5 要求自动 DECLINED，88.1 又要求禁用不改业务状态、归档只读 | 必须确定全局互斥是否允许极小范围 SYSTEM 更新，或在禁用恢复时补结算；归档还需定义是否允许携带 PENDING。无论选哪种，Candidate 全局接受指针必须阻止二次接受，禁用/归档不得发信或递补。未经确认不得私自引入状态修改例外 | P0；阻塞 P5 跨状态联动验收 |
| D2 | 谁可以归档、哪些状态可归档、已有 PENDING 如何处理 | 建议 OWNER 发起且仅在无 PENDING 时归档，平台只管理禁用/激活；这是建议，需要固定后再实现。避免归档隐式撤销正式 Offer | P2 生命周期实现前 |
| D3 | 特殊重新发放是否适用于主动放弃、是否允许绕过 AUTO 排名 | 建议仅 OWNER 对本活动 DECLINED/EXPIRED 执行、有原因和审计；全局已接受者始终拒绝，不扩展“撤销已接受/换活动”流程。AUTO 的负责人例外范围需明确 | P5 特殊接口实现前 |
| D4 | 重新激活后的“人工触发补发”具体指恢复滚动、补齐空额，还是逐封重发 | 建议 AUTO 提供“恢复并按 rank 补齐”操作，MANUAL 保持逐人发放；旧 CANCELLED 邮件不自动恢复。所有后续自动入口读取同一恢复标记 | P2/P5 恢复流程实现前 |
| D5 | 现有数据库是否含有效业务数据、旧接口是否已有调用方 | 先盘点再确定迁移/兼容方案；缺失 student_id、score 时要求可靠补录，不能通过删库或猜测数据完成迁移 | P1 迁移方案实施前 |
| D6 | 预期候选人数、并发确认量、同步 XLSX 最大规模和统计刷新时效 | 保持需求规定的即时 XLSX；按实际量级确定分页、查询索引、超时和容量验收目标，不擅自改成异步导出 | P6 实现与 P8 性能验收前 |

其他技术风险的处理方式：

- 跨活动接受是本系统最高一致性风险。分布式锁不能替代数据库事务，发放与接受之间的竞态必须一起处理。
- 现有模型缺少学号与成绩，不具备无损自动迁移的充分信息；代码注释中“从空库开始”不能作为实际环境证据。
- 邮件与数据库不能形成一个原子事务，明确至少一次投递语义，通过业务幂等和状态检查保证重复邮件无录取副作用。
- 归档和冻结属于不可逆业务操作，应在界面提供明确确认及影响说明；不添加隐式解冻、恢复终态或删除活动接口。
- 存量库迁移前备份，失败时先恢复代码和数据库兼容状态；不要把删除新列或覆盖旧数据当作通用回退手段。

## 9. 命令执行约束与后续验收命令

遵守当前任务提供的 AGENTS.md 约束：**完成代码编写或修改后立即停止，不自行执行任何命令。只有用户明确要求某个具体命令或显式批准该命令后，才执行。** 本计划中的测试、检查、安装、迁移、构建和部署条目均不是命令执行授权。

本轮只完成仓库阅读和本文档写入，没有安装依赖、格式化、测试、编译、构建、启动应用、连接业务服务或运行验收。后续每个实现批次交付时列出改动、未验证项及建议命令；用户可以自行执行，或明确授权后再执行。

以下命令仅供后续批准使用，**目前均未运行**。先将终端切换到表中目录，再执行对应命令；依赖尚未准备、测试尚未补齐时不能将空测试结果解释为验收通过。

| 工作目录 | 待授权命令 | 预期用途及前提 |
| --- | --- | --- |
| `D:\MyWebProject\rollin\rollin-backend` | `go test ./...` | 编译相关包并运行已补齐的 Go 测试；数据库/Redis 集成测试需有隔离环境及测试配置 |
| `D:\MyWebProject\rollin\rollin-backend` | `go test -race ./...` | 检查可执行测试中的 Go 数据竞态；需宿主 Go race/CGO 工具链支持，且不能替代数据库并发验收 |
| `D:\MyWebProject\rollin\rollin-backend` | `go vet ./...` | 执行 Go 静态分析，不代表业务正确性已验证 |
| `D:\MyWebProject\rollin\rollin-frontend` | `npm ci` | 按现有 lockfile 准备依赖，会修改本地依赖目录，必须单独授权；新增依赖后先保证清单与锁文件一致 |
| `D:\MyWebProject\rollin\rollin-frontend` | `npm run lint` | 执行仓库已有 ESLint 脚本 |
| `D:\MyWebProject\rollin\rollin-frontend` | `npm run build` | 执行仓库已有 TypeScript 构建检查并生成 Vite 生产产物 |

当前前端没有 test 脚本，因此不预设 `npm test` 可以使用。数据库迁移、服务启动、端到端测试和部署命令在对应方案/脚本完成、目标环境明确后再给出具体命令并取得授权，不在本轮执行。

## 10. 首个实现批次建议

进入实现时先处理 P0，再交付 P1 的模型与迁移设计。第一批应优先解决活动账户作用域、Candidate 学号身份、Application 活动内资料、独立状态类型以及 Offer 有效记录唯一约束，同时固定 D1—D5 的相关边界。

这一批结束时交付可审阅的代码和迁移说明，并按命令限制停止；获得具体检查命令授权后再验证。不要先沿用旧模型扩展页面，否则账户、排名和 Offer API 变化会造成重复实现。
