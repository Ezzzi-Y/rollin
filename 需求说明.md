# 滚动录取平台完整需求说明

## 1. 平台概述

本平台运行于高校社团、学生组织等招新场景，用于完成招新过程中的：

- 候选人名单导入
- 候选人评分与排名
- Offer 发放
- Offer 在线确认
- Offer 主动放弃
- Offer 超时处理
- 自动滚动递补
- 手动 Offer 发放
- 邮件通知
- 实时统计
- 数据导出
- 活动与权限管理

平台支持同时运行多个独立的招新活动。

每个活动拥有独立的：

- 活动成员
- 候选名单
- 排名
- Offer
- SMTP 配置
- 邮件模板
- 录取容量
- Offer 发放模式
- 统计数据
- 活动配置

因此，可以将一个 Activity 视为一个租户。

不同 Activity 之间的业务数据及权限原则上严格隔离。

但 Candidate 为平台级共享实体，不受 Activity 数据隔离限制，因为同一个候选人可以同时参加多个活动。

本平台只服务当前一届招新。

每届招新完成后会清理上一届业务数据，因此当前版本不需要额外设计年度、届次、RecruitmentCycle 等概念。

------

# 2. 核心实体

当前系统主要包含：

- User
- Activity
- ActivityMember
- Candidate
- Application
- Offer
- ImportToken
- InviteToken
- MailTask
- AuditLog

关系可以理解为：

User
↓
ActivityMember
↓
Activity

Candidate
↓
Application
↓
Activity

Application
↓
Offer

Offer
↓
MailTask

Activity
↓
ImportToken

------

# 3. Activity

Activity 是平台最重要的业务隔离单位。

例如：

- 技术部招新
- 宣传部招新
- 外联部招新

当前版本不额外引入“组织”“部门”“学院”等实体。

Activity 本身就是最高业务单位。

Activity 至少包含：

- id
- title
- quota
- offer_mode
- status
- ranking_frozen
- offer_expire_duration
- offer_success_message
- created_at
- updated_at

其中：

quota：

录取人数。

offer_mode：

Offer 发放模式。

例如：

AUTO
MANUAL

status：

活动状态。

例如：

ACTIVE
DISABLED

ranking_frozen：

排名是否已经冻结。

------

# 4. 当前不引入的业务字段

当前版本不设计：

- 报到时间
- 报到地点
- 组织名称
- 部门层级
- 年度
- 招新周期
- 复杂报名信息

后续确有需要时再扩展。

Offer 确认成功后支持配置一段自定义提示信息即可。

------

# 5. Activity 状态

活动至少存在以下状态：

ACTIVE

正常运行。

DISABLED

活动被平台封禁。

当 Activity 被 DISABLED 后：

1. 该 Activity 的活动负责人无法进入活动。
2. 活动管理员无法进入活动。
3. 已发出的 Offer Token 链接全部拒绝访问。
4. 停止发送该 Activity 的所有邮件。
5. Mail Worker 不得继续发送该 Activity 的待发送邮件。
6. 停止该 Activity 的自动滚动递补。
7. 停止该 Activity 的 Offer 超时任务。
8. 保留现有业务数据。

超级管理员可以重新激活 Activity。

------

# 6. 用户 User

User 是平台级实体。

用户通过：

邮箱 + 密码

进行登录。

同一个邮箱只对应一个平台 User。

但一个 User 可以参与多个 Activity。

例如：

[user@example.com](mailto:user@example.com)

可以：

- 在 Activity A 中担任活动负责人
- 在 Activity B 中担任活动管理员

因此：

User 本身不直接保存 Activity 角色。

Activity 内角色通过 ActivityMember 表达。

------

# 7. RBAC

平台使用 RBAC 权限模型。

当前设置三类角色：

- 超级管理员
- 活动负责人
- 活动管理员

其中超级管理员是平台级角色。

活动负责人和活动管理员是 Activity 级角色。

前端需要根据当前角色渲染不同：

- 菜单
- 页面
- 按钮
- 操作入口

但前端隐藏按钮仅用于改善用户体验。

所有权限必须由后端再次检查。

不能依赖前端权限实现真正的安全控制。

------

# 8. 超级管理员

全平台只存在一个超级管理员。

系统首次部署时，根据环境变量初始化超级管理员账户。

例如：

SUPER_ADMIN_EMAIL

以及对应初始化凭证。

超级管理员主要管理平台和租户生命周期。

拥有：

- 登录平台后台
- 创建 Activity
- 查看 Activity 列表
- 查看 Activity 数量
- 创建活动负责人
- 禁用活动负责人
- 激活 Activity
- 封禁 Activity
- 配置平台 SMTP
- 配置平台级参数

超级管理员原则上不得访问 Activity 内部业务数据。

不得查看：

- 候选人名单
- Candidate 分数
- Candidate 排名
- Offer
- Offer 状态
- 最终录取结果
- Activity 内部详细业务统计

即：

超级管理员管理平台。

活动负责人管理具体招新业务。

------

# 9. 活动负责人

活动负责人是 Activity 内最高权限角色。

活动负责人拥有：

- 查看 Activity 仪表盘
- 查看候选人
- 查看排名
- 查看 Offer
- 查看录取情况
- 查看邮件状态
- 设置录取容量 quota
- 配置 Offer 发放模式
- 配置 Offer 有效时间
- 配置邮件模板
- 配置 Offer 确认成功提示
- 配置 Activity SMTP
- 创建活动管理员
- 禁用活动管理员
- 创建 Import Token
- 吊销 Import Token
- 启动正式录取
- 在 MANUAL 模式下进行人工 Offer 发放
- 导出业务数据
- 查看审计日志
- 执行负责人级特殊业务操作

活动负责人可以修改 quota。

但是：

如果当前已经存在：

ACCEPTED + PENDING = N

则不得将 quota 修改为小于 N 的值。

------

# 10. 活动管理员

活动管理员拥有日常业务管理权限。

包括：

- 查看 Activity 仪表盘
- 查看 Candidate
- 查看 score
- 查看 rank
- 查看 Offer
- 查看邮件发送情况
- 数据导出
- 邮件模板配置
- Offer 成功提示信息配置
- 对发送失败的邮件执行重试
- 在 MANUAL 模式下手动选择 Candidate 发放 Offer

活动管理员不得：

- 创建活动管理员
- 禁用活动管理员
- 创建活动负责人
- 修改 quota
- 管理平台 SMTP
- 修改 Activity 租户级高权限设置
- 查看其他 Activity 数据

在 AUTO 模式下：

活动管理员不得随意手动选择候选人发 Offer。

系统按照 rank 自动滚动。

管理员主要负责监控。

------

# 11. ActivityMember

ActivityMember 用于建立：

User 与 Activity

之间的关系。

至少包含：

- id
- user_id
- activity_id
- role
- status
- created_at
- updated_at

role：

OWNER
ADMIN

或者其他等价命名。

同一个 User 可以在多个 Activity 中拥有不同角色。

------

# 12. 平台 SMTP

平台 SMTP 由超级管理员配置。

平台 SMTP 代表整个系统。

主要用于：

- 活动负责人邀请
- 平台级账户邮件
- 平台系统通知

平台 SMTP 与具体 Activity 无关。

------

# 13. Activity SMTP

每个 Activity 独立配置自己的 SMTP。

由活动负责人负责配置。

Activity SMTP 用于：

- Offer 邮件
- 活动管理员邀请邮件
- 当前 Activity 业务通知

例如：

Activity A 可以使用：

[a@example.com](mailto:a@example.com)

Activity B 可以使用：

[b@example.com](mailto:b@example.com)

互不影响。

Activity 未配置有效 SMTP 时：

不得执行依赖邮件发送的相关业务。

SMTP 密码不得明文保存。

需要使用服务端密钥进行加密存储。

------

# 14. Activity 负责人邀请

创建 Activity 时：

不要求必须同时创建活动负责人。

超级管理员可以之后单独创建负责人。

流程如下：

超级管理员填写：

- 姓名
- 邮箱
- Activity

↓

如果邮箱不存在 User：

创建待激活 User。

如果邮箱已经存在 User：

直接复用。

↓

创建 ActivityMember

↓

生成一次性 Invite Token

↓

使用平台 SMTP 发送邀请邮件

↓

负责人点击邮件链接

↓

设置密码

↓

Invite Token 失效

↓

完成账户激活

↓

登录系统

如果 User 已经有密码：

后续加入新的 Activity 时，可以不要求重新创建密码，具体交互可以后续简化。

------

# 15. 活动管理员邀请

活动负责人可以创建活动管理员。

流程与负责人邀请类似：

活动负责人填写：

- 姓名
- 邮箱

↓

创建或复用 User

↓

创建 ActivityMember

↓

生成 Invite Token

↓

使用 Activity SMTP 发送邮件

↓

用户完成账户设置

↓

登录当前 Activity

------

# 16. Candidate

Candidate 是平台级实体。

不同 Activity 之间共享 Candidate。

同一个自然人在当前平台生命周期内只存在一份 Candidate。

Candidate 至少包含：

- id
- student_id
- name
- email
- accepted_offer_id
- created_at
- updated_at

其中：

student_id

是候选人的平台级唯一标识。

推荐使用：

- 学号
- 学校统一身份 ID
- 外部招新系统 User ID

不能依靠姓名判断 Candidate 是否为同一个人。

------

# 17. Candidate 跨 Activity

一个 Candidate 可以同时参加多个 Activity。

例如：

张三可以同时参加：

- 技术部
- 宣传部
- 外联部

因此 Candidate 与 Activity 是多对多关系。

该关系通过 Application 表达。

------

# 18. Application

Application 表示：

“某个 Candidate 在某个 Activity 中的候选记录”。

至少包含：

- id
- activity_id
- candidate_id
- score
- rank
- status
- created_at
- updated_at

需要建立唯一约束：

UNIQUE(activity_id, candidate_id)

保证同一个 Candidate 在一个 Activity 中只有一条 Application。

------

# 19. Application 状态

Application 可以根据具体实现设计状态。

例如：

WAITING
PENDING
ACCEPTED
DECLINED
EXPIRED
INELIGIBLE

或者也可以让 Offer 承担更多状态信息。

但业务上必须能够明确判断：

- 是否还在候补
- 是否已经发 Offer
- 是否已接受
- 是否已放弃
- 是否已过期
- 是否由于接受其他 Activity Offer 而失去录取资格

具体数据库状态设计后续单独确定。

------

# 20. 名单导入

由于不同 Activity 可能有完全不同的：

- 报名系统
- 面试系统
- 评分方式
- 外部程序

因此平台提供独立名单导入 API。

名单导入接口：

不使用管理员 Session + Cookie。

而使用 Activity 级 Import Token。

------

# 21. Import Token

Import Token 由活动负责人创建。

Import Token 并不是一次性 Token。

一个 Import Token 可以重复调用。

每次名单导入 API 请求：

只允许导入一个 Candidate。

例如：

POST /api/import/candidates

Authorization: Bearer <import_token>

请求示例逻辑：

student_id
name
email
score

Import Token 具备：

- 绑定唯一 Activity
- 只能访问名单导入接口
- 可以重复调用
- 可以设置过期时间
- 可以由活动负责人主动吊销
- 使用密码学安全随机数生成
- 数据库只保存 Token Hash

推荐记录：

- id
- activity_id
- token_hash
- name
- status
- created_at
- expires_at
- revoked_at
- last_used_at

------

# 22. Import Token 安全

Import Token 不允许放在 URL。

禁止：

/api/import?token=xxxx

推荐：

Authorization: Bearer

避免 Token 被记录在：

- Nginx URL 日志
- 浏览器历史
- Referer
- 监控系统 URL
- 代理日志

数据库不得保存原始 Token。

只保存 Hash。

------

# 23. 单人导入

名单导入接口一次请求只允许提交一个 Candidate。

例如：

POST /api/import/candidates

请求：

{
student_id,
name,
email,
score
}

不提供：

一次提交几十、几百人的批量 JSON 接口。

外部系统如需导入 100 人：

调用该接口 100 次。

------

# 24. 名单导入幂等

名单导入必须支持外部系统重试。

例如：

外部系统发送 Candidate A。

服务器已经成功写入。

但是网络中断，调用方没有收到成功响应。

调用方随后再次提交 Candidate A。

系统不得产生重复数据。

因此：

Candidate 根据 student_id 全局唯一。

Application 设置：

UNIQUE(activity_id, candidate_id)

重复调用时必须：

- 返回已有结果
- 或进行明确的幂等更新

不得重复创建 Application。

------

# 25. 名单导入阶段

正式录取启动前：

Activity 处于名单准备阶段。

此时：

- 可以继续调用 Import API
- 可以新增 Candidate
- 可以修改未冻结的排名
- 可以调整同分 Candidate 顺序

名单导入阶段不需要立即确定最终 rank。

系统可以根据 score 动态重算。

------

# 26. score

Candidate 在某个 Activity 的成绩保存在 Application 中。

score 使用：

INT

即可。

例如：

100
95
89
72

当前不需要：

FLOAT
DECIMAL

------

# 27. rank

Application 额外保存：

rank

rank 是正式录取阶段唯一使用的录取顺序依据。

score 用于：

- 初始排序
- 展示
- 审计

rank 用于：

- 首次 Offer 发放
- 自动滚动
- 候补顺序

------

# 28. 初始排名生成

名单准备阶段：

系统按照：

score DESC

生成初始排名。

例如：

95 张三
91 李四
90 王五
90 赵六
85 钱七

初始 rank：

1 张三
2 李四
3 王五
4 赵六
5 钱七

------

# 29. 同分 Candidate

同分 Candidate 的业务裁决发生在正式录取之前。

例如：

A 90
B 90
C 90

系统可以先自动生成：

10 A
11 B
12 C

活动人员之后可以对同分 Candidate 的内部顺序进行调整：

10 C
11 A
12 B

允许：

同分组内调换 rank。

不允许：

跨 score 修改排序。

例如：

A 91
B 90

不得让：

B.rank < A.rank

------

# 30. 同分问题处理原则

正式录取阶段不再处理“同分裁决”。

因为：

同分问题已经在排名阶段通过 rank 解决。

正式录取以后：

系统完全不关心 Candidate 是否同分。

只关心：

rank

因此：

如果 quota = 20

系统直接处理：

rank 1 ～ rank 20

不存在：

“第 20 名和第 21 名同分，所以暂停”

这种逻辑。

因为 rank 在正式录取前已经代表最终业务顺序。

------

# 31. 排名冻结

第一次正式启动录取时：

Activity 排名立即冻结。

ranking_frozen = true

此后：

- 不允许修改 score
- 不允许修改 rank
- 不允许重新生成 rank
- 不允许继续调用 Import API 新增 Candidate
- 不允许删除参与录取的 Application

从这一刻起：

rank 成为不可变业务数据。

------

# 32. 正式录取阶段

整体流程：

名单导入
↓
系统生成初始排名
↓
调整同分顺序
↓
确认名单
↓
启动正式录取
↓
冻结排名
↓
Offer 发放
↓
候选人接受 / 放弃 / 超时
↓
自动或人工递补
↓
录取完成

------

# 33. Offer 发放模式

Activity 支持两种模式：

AUTO

自动滚动录取。

MANUAL

人工发放。

正式录取开始前由活动负责人配置。

正式录取开始后原则上不允许随意切换模式。

如后续需要支持切换，应设计专门业务规则。

V1 暂不考虑运行中任意切换。

------

# 34. quota

每个 Activity 存在固定录取容量：

quota

例如：

quota = 20

核心约束：

ACCEPTED + PENDING <= quota

任意时刻都不能突破该规则。

------

# 35. Offer

Offer 表示：

平台已经正式向 Candidate 发出的录取邀请。

Offer 至少包含：

- id
- application_id
- token_hash
- status
- expires_at
- sent_at
- accepted_at
- declined_at
- expired_at
- created_at
- updated_at

------

# 36. Offer 状态

Offer 至少包含：

PENDING

已发 Offer，等待 Candidate 确认。

占用 quota。

ACCEPTED

Candidate 已接受。

占用 quota。

DECLINED

Candidate 已主动放弃。

或者由于接受其他 Activity 的 Offer，被自动视为放弃。

不占用 quota。

EXPIRED

Offer 已超过截止时间。

不占用 quota。

------

# 37. Offer 正式发出后不可撤回

Offer 正式进入 PENDING 后：

管理员不能任意撤回。

即：

PENDING

不能因为管理员改变主意：

PENDING -> WAITING

这是为了保证已经发送给 Candidate 的正式 Offer 具有可信度。

正常终态只有：

ACCEPTED
DECLINED
EXPIRED

特殊异常必须通过负责人级人工干预流程解决，并记录 AuditLog。

------

# 38. AUTO 模式首次发放

假设：

quota = K

启动正式录取时：

系统按照：

rank ASC

向当前排名最靠前且具备资格的 K 个 Candidate 创建 Offer。

例如：

quota = 20

默认向：

rank 1 ～ rank 20

发 Offer。

创建完成后：

这些 Offer 进入：

PENDING

并占用 20 个名额。

------

# 39. AUTO 模式滚动递补

如果 Candidate：

主动 DECLINED

或者：

Offer EXPIRED

则释放一个 quota。

例如：

quota = 20

ACCEPTED = 12

PENDING = 8

当前占用：

20

其中一个 PENDING 变为 DECLINED：

ACCEPTED = 12

PENDING = 7

占用：

19

系统寻找：

rank 最小的 WAITING Application

为其创建 Offer。

恢复：

ACCEPTED + PENDING = 20

------

# 40. AUTO 模式只看 rank

自动滚动过程中：

不再读取 score 进行业务判断。

核心查询逻辑类似：

找到：

status = WAITING

按照：

rank ASC

取第一个满足录取资格的 Candidate。

因此：

score 只保留为原始评估数据。

rank 才是正式录取顺序。

------

# 41. MANUAL 模式

MANUAL 模式下：

系统不自动补录。

如果有人：

DECLINED

或者：

EXPIRED

则只是释放 quota。

管理员随后人工选择 Candidate 发 Offer。

MANUAL 模式允许：

不按照 rank 顺序发 Offer。

例如：

可以先给 rank 10。

再给 rank 3。

但仍然必须满足：

ACCEPTED + PENDING <= quota

容量已满时：

不得继续发 Offer。

------

# 42. Candidate 可以同时收到多个 Activity Offer

Candidate 是全局实体。

因此：

同一个 Candidate 可以同时拥有多个 Activity 的 PENDING Offer。

例如：

张三：

技术部 -> PENDING
宣传部 -> PENDING
外联部 -> PENDING

这是合法状态。

平台不会因为某 Activity 已向张三发 Offer，就阻止其他 Activity 发 Offer。

前提是：

张三尚未 ACCEPTED 任何 Offer。

------

# 43. Candidate 只能接受一个 Offer

在当前平台生命周期内：

每个 Candidate 最多只能拥有一个 ACCEPTED Offer。

Candidate 尚未接受任何 Offer 时：

可以同时拥有多个 PENDING Offer。

一旦接受其中一个：

其他 PENDING Offer 全部自动视为主动放弃。

------

# 44. accepted_offer_id

Candidate 建议维护：

accepted_offer_id

如果：

accepted_offer_id IS NULL

表示：

Candidate 当前尚未接受任何 Activity Offer。

如果：

accepted_offer_id IS NOT NULL

表示：

Candidate 已经最终加入某一个 Activity。

该字段作为平台级最终录取状态。

------

# 45. 接受 Offer 的跨 Activity 行为

假设：

张三拥有：

Activity A -> PENDING

Activity B -> PENDING

Activity C -> PENDING

张三接受 A：

A：

PENDING -> ACCEPTED

Candidate：

accepted_offer_id = A.offer_id

同时：

B：

PENDING -> DECLINED

C：

PENDING -> DECLINED

B 和 C 的 DECLINED：

业务上视为 Candidate 主动放弃。

------

# 46. 其他 Activity 释放容量

Candidate 接受一个 Offer 后：

其他 Activity 中 PENDING Offer 自动 DECLINED。

这会释放对应 Activity 的容量。

如果这些 Activity 使用：

AUTO

则需要继续执行自己的滚动递补。

如果使用：

MANUAL

则只释放容量。

等待管理员人工发送新的 Offer。

------

# 47. 接受后禁止再发新 Offer

一旦：

Candidate.accepted_offer_id IS NOT NULL

其他 Activity 不允许再向该 Candidate 创建新的 Offer。

即：

Candidate 接受某个 Activity 后：

正式退出其他 Activity 的后续录取竞争。

------

# 48. Candidate 接受 Offer 使用分布式锁

Candidate 接受 Offer 是平台级跨 Activity 操作。

必须使用分布式锁保证：

同一个 Candidate 同一时刻只能处理一个 Accept 请求。

推荐锁 Key：

candidate:{candidate_id}:accept-offer

例如：

candidate:12345:accept-offer

------

# 49. Accept Offer 并发流程

请求：

POST /api/public/offers/{token}/accept

处理流程：

获取：

candidate:{candidate_id}:accept-offer

分布式锁

↓

开启 MySQL Transaction

↓

重新读取 Candidate

↓

检查：

accepted_offer_id

如果非 NULL：

说明 Candidate 已经接受其他 Offer。

当前请求失败。

↓

检查当前 Offer：

status 是否仍然为 PENDING

↓

如果合法：

当前 Offer：

PENDING -> ACCEPTED

↓

Candidate：

accepted_offer_id = 当前 Offer ID

↓

查询 Candidate 其他 Activity 中：

status = PENDING

的 Offer。

↓

全部：

PENDING -> DECLINED

↓

记录被释放 quota 的 Activity

↓

COMMIT

↓

对受影响的 AUTO Activity 执行滚动补录

↓

释放 Candidate 分布式锁

------

# 50. 分布式锁不能代替数据库状态

分布式锁用于：

降低并发冲突。

但业务最终状态必须由 MySQL 保证。

因此 Accept Transaction 中仍必须检查：

accepted_offer_id

不能认为：

“拿到了分布式锁就一定没问题”。

需要考虑：

- 锁过期
- 网络抖动
- 服务实例暂停
- Redis 故障
- 请求重试

因此：

分布式锁是第一层。

数据库事务和业务状态是第二层。

------

# 51. Offer Token

每个 Offer 生成一个独立 Token。

邮件中发送：

https://t.xxx.xxx/o/{token}

t.xxx.xxx 是对外 Candidate 页面域名。

该域名为临时招新域名。

本届招新结束后可以直接下线。

------

# 52. Offer Token 安全

Offer Token：

- 使用密码学安全随机数
- 建议至少 32 Bytes
- 不可预测
- 不得使用自增 ID
- 不得使用 offer_id
- 不得使用 candidate_id

数据库只保存：

SHA-256(token)

浏览器携带：

原始 Token。

服务器收到后：

SHA-256(token)

再查询：

token_hash

------

# 53. Candidate Offer 页面

Candidate 无需：

注册
登录
Session

只需要通过邮件 Token 打开：

https://t.xxx.xxx/o/{token}

页面展示：

- Candidate 姓名
- Activity 标题
- Offer 正文
- Offer 截止时间
- 自定义说明
- 接受 Offer
- 放弃 Offer

页面视觉质量应较高。

该页面属于组织对 Candidate 的正式交互页面。

不能简单设计成普通后台表单。

------

# 54. 访问页面本身不能改变状态

打开：

GET /o/{token}

不能产生任何业务状态修改。

因为：

邮件安全服务
反垃圾系统
URL Scanner

可能自动访问链接。

因此：

仅访问页面绝不能等价于接受 Offer。

------

# 55. 接受 Offer

Candidate 主动点击：

接受 Offer

之后才执行 Accept API。

前端建议再进行一次确认。

Accept API 必须幂等。

例如：

第一次 Accept 成功。

随后浏览器重复提交。

系统不得再次触发：

- 其他 Offer 重复 DECLINE
- 多次滚动补录
- 多次记录业务事件

------

# 56. 放弃 Offer

Candidate 点击：

放弃 Offer

需要二次确认。

提示例如：

“确认放弃本次录取资格吗？放弃后不可恢复。”

确认后：

PENDING -> DECLINED

释放 Activity quota。

AUTO 模式：

继续滚动递补。

MANUAL 模式：

等待管理员。

DECLINED 后 Candidate 不得自行恢复。

------

# 57. Offer 超时

Offer 保存：

expires_at

系统后台定时检查：

status = PENDING

AND

expires_at <= NOW()

则：

PENDING -> EXPIRED

释放 quota。

AUTO：

自动滚动。

MANUAL：

不自动补录。

------

# 58. Offer 超时后的重发

需要严格区分：

“邮件重发”

和：

“Offer 重新发放”。

------

# 59. 邮件重发

如果：

Offer 仍然是 PENDING

只是：

- SMTP 失败
- Candidate 没收到邮件
- 管理员希望再次发送邮件

则可以：

重新发送相同 Offer 邮件。

Offer：

Token 不变。

状态不变。

截止时间原则上不变。

------

# 60. EXPIRED Offer 不允许普通重发

如果：

Offer 已经 EXPIRED

则代表：

该录取资格已经失效。

其名额甚至可能已经递补给其他 Candidate。

因此：

不得通过“重新发送邮件”把：

EXPIRED -> PENDING

如果确实需要重新给予 Candidate 录取机会：

应当执行负责人级特殊业务操作。

创建新的 Offer。

而不是恢复旧 Offer。

------

# 61. 邮件发送与业务事务分离

禁止：

BEGIN

创建 Offer

调用 SMTP

COMMIT

因为 SMTP：

- 可能慢
- 可能失败
- 可能超时

不能长期占用 MySQL Transaction。

------

# 62. MailTask

系统使用数据库 MailTask 实现可靠邮件发送。

业务 Transaction：

BEGIN

创建 Offer

创建 MailTask

COMMIT

随后 Mail Worker：

扫描 MailTask

↓

调用 SMTP

↓

成功：

SENT

失败：

记录错误并重试

------

# 63. MailTask 状态

建议包含：

PENDING

待发送。

SENDING

正在发送。

SENT

发送成功。

FAILED

达到失败条件。

字段例如：

- id
- activity_id
- offer_id
- recipient
- template_type
- status
- retry_count
- next_retry_at
- last_error
- sent_at
- created_at
- updated_at

------

# 64. Mail Worker

Mail Worker 发送邮件前：

必须再次检查 Activity 是否 ACTIVE。

如果 Activity 已 DISABLED：

不得发送。

因此：

封禁 Activity 可以立即停止尚未发送的业务邮件。

------

# 65. AUTO Activity 递补并发安全

一个 Activity 可能同时发生：

- Candidate 主动放弃
- Offer 超时
- Candidate 接受其他 Activity Offer
- 多个 Candidate 同时放弃

因此：

同一个 Activity 的滚动递补必须串行化。

建议使用：

Activity 级数据库锁

或者等价事务控制。

例如：

SELECT *
FROM activity
WHERE id = ?
FOR UPDATE;

确保同一个 Activity 同时只有一个滚动事务进行。

------

# 66. AUTO 滚动核心逻辑

概念流程：

BEGIN

锁定 Activity

↓

检查：

Activity = ACTIVE

↓

计算：

occupied = ACCEPTED + PENDING

↓

如果：

occupied >= quota

结束。

↓

找到：

rank 最小的 WAITING Application

↓

检查对应 Candidate：

accepted_offer_id 是否为 NULL

如果已经接受其他 Activity：

则该 Candidate 不再具备录取资格。

跳过。

↓

创建 Offer

↓

Application 进入已发 Offer 状态

↓

创建 MailTask

↓

occupied + 1

↓

如果：

occupied < quota

继续查找下一个 WAITING Candidate。

↓

直到：

occupied = quota

或者：

没有 Candidate

↓

COMMIT

------

# 67. Candidate 已被其他 Activity 录取

AUTO 滚动时可能遇到：

rank 21 的 Candidate 已经 ACCEPTED 其他 Activity。

此时：

当前 Activity 不应该给其发送 Offer。

系统应：

跳过该 Candidate。

并明确标记其在当前 Application 中已经不再具有录取资格。

然后继续：

rank 22

以此类推。

------

# 68. MANUAL 模式发 Offer 前检查

管理员人工选择 Candidate 发 Offer 时：

后端必须检查：

1. Activity 是否 ACTIVE。
2. ranking 是否已冻结。
3. Activity 是否为 MANUAL。
4. 当前 Candidate 是否存在有效 Application。
5. Candidate 是否已经 accepted_offer_id != NULL。
6. Candidate 是否已经拥有当前 Activity 有效 Offer。
7. ACCEPTED + PENDING 是否仍小于 quota。

任一条件不符合：

拒绝发放。

------

# 69. Offer 截止时间

Activity 可以配置 Offer 默认有效期限。

例如：

24 小时
48 小时
72 小时

创建 Offer 时：

expires_at

根据 Activity 配置计算。

后续可以支持负责人针对特殊 Offer 自定义截止时间。

V1 可以先统一使用 Activity 默认配置。

------

# 70. 管理后台仪表盘

Activity Dashboard 至少展示：

- quota
- 已接受 ACCEPTED
- 待确认 PENDING
- 已放弃 DECLINED
- 已超时 EXPIRED
- 当前 WAITING 数量
- 已发 Offer 总数
- 当前录取占用量
- 邮件发送失败数量
- Activity 当前状态
- Offer 发放模式
- 排名是否已经冻结

例如：

录取容量：20

已接受：12

待确认：8

当前占用：20

候补：30

放弃：5

超时：2

------

# 71. Candidate 列表

后台 Candidate/Application 列表至少展示：

- rank
- 姓名
- 邮箱
- student_id
- score
- Application 状态
- Offer 状态
- Offer 截止时间
- 邮件状态

支持：

- 查询
- 状态筛选
- 排序
- 分页
- 数据导出

------

# 72. 数据导出

活动负责人和活动管理员可以导出：

当前 Activity

的业务数据。

不得导出其他 Activity 数据。

导出可以包括：

- Candidate ID
- 姓名
- 邮箱
- score
- rank
- Application 状态
- Offer 状态
- Offer 发送时间
- Accept 时间
- Decline 时间
- Expire 时间

------

# 73. 超级管理员后台

超级管理员前端主要包括：

活动管理

- Activity 列表
- 创建 Activity
- 激活 Activity
- 封禁 Activity

负责人管理

- 创建负责人
- 禁用负责人

平台配置

- 平台 SMTP
- 平台参数

平台统计

- Activity 数量
- ACTIVE Activity 数量
- DISABLED Activity 数量

不展示 Activity 内部业务内容。

------

# 74. 活动负责人后台

活动负责人页面可以包括：

Dashboard

Candidate

Ranking

Offer

Mail

Activity Settings

SMTP

Admin Management

Import Token

Export

Audit Log

------

# 75. 活动管理员后台

活动管理员页面可以包括：

Dashboard

Candidate

Ranking 查看

Offer

Mail

Export

允许修改的模板配置

MANUAL 模式下：

Offer 发放功能。

AUTO 模式下：

主要查看运行结果。

------

# 76. Candidate 前端域名

Candidate Offer 页面使用：

t.xxx.xxx

例如：

https://t.xxx.xxx/o/{token}

该域名只用于：

- Offer 展示
- Offer 接受
- Offer 放弃
- Offer 最终状态展示

不提供：

- 管理员后台
- 管理员登录
- Activity 管理入口

------

# 77. 管理后台域名

管理后台域名不做强约束。

可以使用：

admin.xxx.xxx

或者：

recruit.xxx.xxx

或其他临时域名。

由于本系统用完后可能整体下线：

域名结构无需过度设计。

------

# 78. 后台认证

管理后台使用：

Session + Cookie

登录方式：

邮箱 + 密码

Cookie 应至少配置：

HttpOnly

Secure

合理的 SameSite

管理员 API 通过 Session 判断当前 User。

随后根据：

ActivityMember

继续校验其 Activity 权限。

------

# 79. Public Offer API

Candidate Offer API 不使用管理员 Session。

通过：

Offer Token

进行授权。

例如：

GET /api/public/offers/{token}

POST /api/public/offers/{token}/accept

POST /api/public/offers/{token}/decline

------

# 80. Import API

Import API 不使用：

Session + Cookie。

使用：

Authorization: Bearer

例如：

POST /api/import/candidates

每次：

只允许导入一个 Candidate。

------

# 81. AuditLog

关键后台业务操作需要保存审计日志。

至少记录：

- actor_user_id
- activity_id
- action
- target_type
- target_id
- change_summary
- created_at

自动任务：

actor 可以记为 SYSTEM。

------

# 82. 建议记录 AuditLog 的操作

包括：

- 创建 Activity
- 激活 Activity
- 封禁 Activity
- 创建负责人
- 禁用负责人
- 修改 quota
- 修改 Offer Mode
- 创建管理员
- 禁用管理员
- 创建 Import Token
- 吊销 Import Token
- 启动正式录取
- 调整同分 rank
- 人工发送 Offer
- 特殊 Offer 操作
- 修改邮件模板
- 修改 SMTP 配置

------

# 83. 平台生命周期

完整流程：

部署系统

↓

初始化超级管理员

↓

超级管理员登录

↓

配置平台 SMTP

↓

创建 Activity

↓

创建活动负责人

↓

负责人通过邮件进入系统

↓

配置 Activity SMTP

↓

配置 Activity

↓

创建活动管理员

↓

创建 Import Token

↓

外部系统逐人导入 Candidate

↓

系统按照 score 生成初始 rank

↓

活动人员处理同分内部排序

↓

确认最终排名

↓

启动正式录取

↓

冻结 score 和 rank

↓

根据 AUTO / MANUAL 模式发 Offer

↓

Candidate 通过邮件访问 Offer 页面

↓

接受 / 放弃 / 超时

↓

AUTO Activity 自动滚动递补

或

MANUAL Activity 人工继续发 Offer

↓

最终录取完成

↓

导出数据

↓

活动结束

↓

本届招新结束

↓

清空业务数据

↓

下一届重新部署或重新启用

------

# 84. 核心业务约束

系统实现必须始终满足以下规则：

1. Activity 是主要租户边界。
2. Activity 间业务数据默认严格隔离。
3. Candidate 是平台级共享实体。
4. 同一个 Candidate 可以参加多个 Activity。
5. Candidate 可以同时拥有多个 PENDING Offer。
6. Candidate 最多只能拥有一个 ACCEPTED Offer。
7. Candidate 接受一个 Offer 后，其他 PENDING Offer 自动 DECLINED。
8. Candidate 已 ACCEPTED 后，其他 Activity 不得继续向其发新 Offer。
9. Candidate Accept 必须使用 Candidate 级分布式锁。
10. 分布式锁之外，数据库事务仍需重新检查 accepted_offer_id。
11. 每个 Activity 必须满足：

ACCEPTED + PENDING <= quota

1. AUTO 模式严格按照 rank ASC 发放。
2. AUTO 模式正式录取后不再关心 score。
3. 同分问题必须在正式录取前通过调整 rank 解决。
4. 不允许跨 score 调整 rank。
5. 正式录取启动后 rank 和 score 永久冻结。
6. 排名冻结后不再允许名单导入。
7. MANUAL 模式允许不按照 rank 发 Offer。
8. MANUAL 模式仍不得突破 quota。
9. Offer 一旦正式发放原则上不可撤回。
10. DECLINED Offer 不得由 Candidate 恢复。
11. EXPIRED Offer 不得通过简单邮件重发重新激活。
12. 打开 Offer URL 本身不得修改状态。
13. Offer Accept / Decline API 必须幂等。
14. Import Token 可以重复调用。
15. Import Token 每次请求只能导入一个 Candidate。
16. Import Token 只允许访问指定 Activity 的名单导入能力。
17. Import Token 可以被吊销。
18. Import API 必须保证幂等。
19. Candidate 需要稳定的平台级 student_id。
20. 同一个 Activity 中 Candidate Application 必须唯一。
21. 邮件发送不能位于核心数据库事务中。
22. MailTask 必须支持失败记录及重试。
23. Activity 被封禁后停止所有业务行为。
24. Activity 被封禁后已有 Candidate Token 必须拒绝访问。
25. 同一 Activity 的自动滚动必须进行并发控制。
26. 所有关键权限必须由后端校验。
27. 关键管理操作需要保存审计日志。

------

# 85. 推荐后端技术栈

后端：Go

------

# 86. 推荐前端技术栈

前端与后端独立工程。

前端：

React

TypeScript

Vite

React Router

TanStack Query

Tailwind CSS

shadcn/ui

React Hook Form

Zod

Lucide Icons

前端质量优先级较高。

特别是 Candidate Offer 页面：

应具有正式、简洁、美观的视觉表现。

------

# 87. 推荐工程结构

后端：

rollin-server

前端：

rollin-web

例如：

rollin-server/
cmd/
internal/
auth/
activity/
candidate/
application/
offer/
ranking/
importtoken/
mail/
audit/
worker/
repository/
service/

rollin-web/
src/
pages/
admin/
offer/
components/
api/
hooks/
layouts/
router/
------

# 88. 需求澄清结果（已澄清）

本章记录截至本轮需求澄清已经确认的规则。若本章内容与前文存在冲突，以本章为准。未在本章列出的内容继续沿用前文定义。

## 88.1 Activity 生命周期【已澄清】

1. Activity 状态为：

   - ACTIVE：正常运行。
   - DISABLED：暂时禁用。
   - ARCHIVED：已归档，只读。

2. Activity 不允许删除。

3. 每个 Activity 只设置一个 OWNER；可以设置多个 ADMIN。

4. ARCHIVED Activity 不允许重新激活。Activity 成员仍然可以登录并导出数据，但不得执行任何后台数据修改操作。

5. Candidate 访问 ARCHIVED Activity 的 Offer 页面时可以查看结果，但不得接受或放弃 Offer。

6. DISABLED Activity 的处理规则：

   - 不修改 Offer、Application 等业务状态。
   - 通过 Offer Token 解析出 Activity 后，如果 Activity 已禁用，直接返回“Offer 已失效”。
   - 不因访问失败而修改数据库。
   - 已有状态全部保留。
   - MailTask 直接取消，不再发送。
   - 不自动重新发送 Offer。
   - 重新激活后，未过期的 PENDING Offer 和对应 Token 恢复可用；已经过期的 Offer 仍按过期处理。
   - 重新激活后是否补发新的 Offer，由负责人或管理员手动触发。

7. 超级管理员不提供平台 User 的禁用功能。

## 88.2 账户与邀请【已澄清】

1. 同一个邮箱在不同 Activity 中不视为同一个账户。

2. 不同 Activity 可以创建相同邮箱的独立 User 账户，各自拥有独立密码和 Activity 权限。

3. 登录时必须能够确定目标 Activity。

4. 不提供密码找回功能。

5. 邀请 Token 默认有效期为 72 小时。重复邀请时生成新 Token，旧 Token 失效。

6. 同一 Activity 中已经存在的成员不得重复创建成员关系；需要时可以重新发送邀请。

## 88.3 Candidate、student_id 与 Application【已澄清】

1. 原文中的 external_id 统一改名为 student_id，含义为候选人的学号。

2. student_id 用于识别跨 Activity 的同一 Candidate，并用于 Offer 判重。

3. Candidate 表示平台层面的候选人身份；Application 表示该 Candidate 参加某个 Activity 的活动候选记录。

4. 不同 Activity 的候选人资料相互隔离。name、email、score、rank、Application 状态等属于对应 Activity 的 Application，不要求在不同 Activity 间相同。

5. 同一个 Candidate 可以拥有多个 Application，但同一个 Activity 内只能有一条 Application。

6. student_id 不允许修改。

7. score 必须是正整数，不能为 0 或负数，最大值不超过 INT 类型上限。

8. 排名冻结后禁止继续导入 Candidate，也禁止修改 score。

## 88.4 导入与排名【已澄清】

1. Import API 每次请求仍然只允许导入一个 Candidate，并且必须支持幂等。

2. 每次导入或修改 score 后，Activity 设置“排名待重算”标记。

3. 排名由管理员手动触发重算，不再因导入自动重算。

4. 只要排名待重算标记未清除，就不允许启动正式录取。

5. 初始排名按 score DESC 生成；同分时按导入顺序排列。

6. 管理员可以在同分组内调整顺序，不允许跨 score 调整。

7. 正式录取启动后排名永久冻结，不能撤销启动或恢复修改。

## 88.5 Application 与 Offer 状态【已澄清】

1. Application 使用明确状态保存业务状态，至少包括：

   - WAITING
   - OFFERED
   - ACCEPTED
   - DECLINED
   - EXPIRED
   - INELIGIBLE

2. Candidate 接受一个 Activity 的 Offer 后，其他 Activity 的 PENDING Offer 自动进入 DECLINED，相关 Activity 记录系统审计日志并按模式处理递补。

3. Candidate 已经拥有 accepted_offer_id 后，其他 Activity 不得再向其创建新的普通 Offer。

4. Offer 只能处理一次。重复点击同一个 Offer 时，前端展示“你已经接受了”或对应的已处理结果，不重复执行业务操作。

5. 如确实需要重新给予录取机会，由负责人执行特殊操作：

   - 创建新的 Offer；
   - 旧 Offer 保留原终态；
   - 新 Offer 继续关联原 Application；
   - 必须填写原因并记录 AuditLog。

## 88.6 Offer Token 与邮件发送【已澄清】

1. Offer Token 在 MailTask 实际发送邮件时生成，而不是在创建 Offer 的核心事务中提前生成。

2. 多个不同 Token 可以指向同一个 Offer。

3. 只要 Offer 尚未进入终态，已经生成的有效 Token 都可以访问同一个 Offer；Offer 进入 ACCEPTED、DECLINED、EXPIRED 或其他终态后，所有对应 Token 均不可继续处理。

4. Offer 的截止时间不因邮件重试而重新计算。

5. 邮件发送失败时，系统支持自动重试；达到失败条件后标记 FAILED，管理员可以手动重新排队发送。

6. 邮件重发只重新发送同一个 Offer，不改变 Offer 状态；特殊重新给予录取机会必须创建新的 Offer。

## 88.7 quota 与模式【已澄清】

1. quota 不允许为 0。

2. quota 允许增加或减少，但任何时候都必须满足：

   ACCEPTED + PENDING <= quota

3. AUTO 模式严格按照冻结后的 rank 顺序自动递补。

4. MANUAL 模式不自动递补，由管理员手动选择 Candidate 发放 Offer。

5. 正式录取启动后不允许切换 AUTO 与 MANUAL 模式。

## 88.8 SMTP、导出与清理【已澄清】

1. Activity 未配置有效 SMTP 时，不允许执行依赖 Activity 邮件发送的业务；建议通过 SMTP 测试确认配置有效。

2. 导出格式为 XLSX，采用立即生成并下载的方式。

3. 系统不提供数据库清理功能。整个平台数据的清理由外部运维流程负责，不纳入本系统业务范围。